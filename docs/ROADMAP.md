# Roadmap

Units of work, in order. Each ends with a worklog entry and, where it touches the hardware, a
record with the fence, GPU clock and pixel hash the oops-gl instrument already produces. The
order is chosen so that the one measurement which decides the driver route (D003) is made
before the code that depends on its answer exists.

| unit | what it delivers | gate | state |
|---|---|---|---|
| 1 | the repository: pin, verbs, decisions, this file | `./bin/oops-mesa check` passes; the collection's decision and link gates pass | done 2026-09-14 |
| 2 | **the route measurement**: radeonsi's initial graphics state emitted through oops-gl's submission path, compositor watched, known frame hashed | pass or fail recorded per D003, with the system-log lines | done 2026-09-14: pass, both variants (worklog 002) |
| 3 | the container build: `toolchain/build-mesa.sh`, the meson cross file, the C-library headers staged from the checkout D004 pins, Mesa configured for the target with radeonsi and ACO only, no LLVM, static, one EGL platform | `./bin/oops-mesa build` reaches Mesa's first demand this platform cannot meet, and that demand is the next unit's work list | done 2026-09-14: 48 archives build for the target (worklog 005) |
| 4 | the runtime shim: the bound-name table, the thread-name mapping over the vendor thread API, Mesa's C11-threads layer over it, a host test for the mapping | `src/runtime/threads.c` maps all 26 functions in [the thread surface record](hardware/mesa-thread-surface-fw1240.md) and compiles against the platform's own `pthread.h` (worklog 011); `./bin/oops-mesa test` today runs the winsys host suite, not a thread-mapping suite | threads done 2026-09-14 (`src/runtime/threads.c`, worklog 011); **the rest of the C-runtime surface is named and largely closed as of 2026-09-17** (worklog 042). The link named it - 181 undefined symbols with `--error-limit=0` - and obSCEne swept all of it (`-5b28`), which split the 68 absent names into 35 that are genuinely absent and 33 where this collection's own export census disagrees with the sweep (filed as `-9f41`, not guessed at). The 35 are answered: fourteen arithmetic names plus nine the sweep never reached now come from FreeBSD's own `lib/msun`, built for the target out of the checkout D004 already pins, and the rest are implementations or loud stubs in `src/runtime/`. **No name that is measured absent is still imported**, and 181 undefined symbols are down to 116. The C++ ABI needed no library: Mesa is built `-fno-exceptions` and references neither `__cxa_begin_catch` nor `__gxx_personality_v0` - both came from this repository's own `cxx_support.cpp` being compiled with exceptions it says it does not use - while `__cxa_pure_virtual` is referenced from vtables and is now defined here, since a pure virtual actually being called is a caller's lifetime bug and not a platform question. **Both open questions closed on 2026-09-17 evening** (worklog 045). `-9f41`: dynamic resolution is authoritative, all 33 disputed names are unbindable natively, and the export census that listed them was a **GEN=4** capture - the PS4 backward-compatibility container, whose Orbis libc did export them. So the eleven still imported were a crash waiting for a caller, not a judgement call, and all eleven are now local: the C locale's character tables come from `lib/libc/locale/table.c` staged from the same pin as msun, `time` goes over the exported `clock_gettime`, and the rest are implementations or loud stubs. **The surface is closed, and in the strong sense** (worklog 047): every symbol a Mesa title imports is resolved to an address on the native leg - 98 of them, with zero advisory, zero unmeasured, zero measured-absent-still-imported. 181 undefined symbols are down to 95 and every one binds. The last 34 unknowns were named by `what-is-still-needed.sh` once it stopped trusting the GEN=4 census, swept as `-a4f2` (20 present, 14 absent), and the 14 defined here - `isatty` for the GLSL lexer, `__cxa_atexit` and the ASTC static-init guards, and loud refusals for SPIR-V's `longjmp` and the HUD. `-6b3e`: 16 hardware threads, 14 available to a big-app container, and `sysconf` now reports them, so Mesa sizes its shader-compiler pool at 14 instead of 1 - the available count queried through `cpuset_getaffinity` rather than stated, because the resolution says 12 is assigned under some workloads |
| 5 | the winsys, standalone (an ioctl under libdrm carrying the 19 AMDGPU commands, the generic DRM ones and the syncobj family, D005; the skeleton is in src/winsys and the unimplemented commands refuse loudly): allocate, map, submit, fence over oops-sdk's bindings, exercised by a title that submits the oracle record's stream and gets the oracle record's hash | the oops-sdk oracle record reproduced through the winsys on firmware 12.40 | the startup path is closed as of 2026-09-17: every call from `amdgpu_get_auth` to the end of `ac_query_gpu_info` is answered or refused with the consequence known, tabulated in worklog 021. **It ran on hardware for the first time on 2026-09-17** (worklog 038): thirty-five commands answered through to radeonsi's screen creation. It then stopped three times in `si_init_renderer_string` for three different reasons, all thread-local storage, settled in worklog 040: the platform does not export `_ThreadRuneLocale` (so oops-mesa defines it, null being its correct value), its `__tls_get_addr` exists but faults, and Local Exec cannot be linked into a `-shared` title - leaving **Initial Exec as the only workable TLS model**, which the cross file now asks for. **It cleared the line: radeonsi created a screen on the hardware**, thirty-five commands answered with two refusals the driver tolerated, captured in [the record](hardware/screen-created-fw1240.md) and read in worklog 042. That is this unit's stated milestone met on firmware 12.40. The same capture shows the probe reaching its own `done` and the container then faulting at `rip: 0` on the way out, strictly after the work. That ending is now settled and fixed: obscene `-2e71` resolved that a `big-app` container cannot terminate itself at all - lifecycle belongs to the shell, `_exit` raises `SIGSYS`, and returning transfers to zero for want of a caller frame - so both Mesa titles now print their last line and idle for the host to close (worklog 044). **The whole runtime stack then ran on hardware and the milestone reproduced under it** (worklog 048, [record](hardware/full-stack-parks-fw1240.md)): the same 35 ioctls and the same screen with libm, the locale tables, the C++ ABI, the fourteen `-a4f2` names and the GL entry points all present, and the title parked at `idle and finished` instead of faulting - `pros ps` showing it `SLEEP` at 55 threads. The parking ending's one catch is that `pros close`/`kill`/`restart-ui` cannot then close it (only the shell UI's own Close can), filed as Prosperous `-b1e4`. The gate as written needs submission, and submission sits behind syncobj wait and signal, which D007 leaves unwritten - so **the useful milestone before it is a non-null `pipe_screen`**, which worklog 022 establishes issues no ioctl past that table and allocates no GPU memory on this part |
| 6 | radeonsi bring-up (unit 2 passed; the fallback driver stays written down in D003): a triangle through the **Gallium DRI frontend** and OpenGL 3.3 into the display oops-sdk opens, flipped with the packet flip and retired on fence plus flip token. **Not EGL** - upstream builds it `shared_library()` regardless of `default_library=static`, so a title cannot link it, and the loader underneath is what EGL would have wrapped (D010) | a title draws and hashes a known frame; the swap costs milliseconds, not the 508 ms the CPU path costs today | D009 fixes the buffer direction, D010 the entry point, worklog 031 proves the layout and 033 the format. **The shim is written and compiles as of 2026-09-17** - `src/platform/dri_loader.c`, with `oops_gl_create`/`present`/`destroy`, the format carried opaquely out of the chosen config (worklog 039, which closes 037's open argument). **It is in the build as of 2026-09-17** (worklog 041): a title links the DRI frontend, places every import and packages at 28.2 MB. The 55 unplaced symbols 039 measured are all accounted for - 24 driver descriptors came from a target object the build already produced but `link-order.txt` filtered out, 4 were the software rasteriser's shared-memory calls, and the last was a weak C++ thread-local initialiser that cannot resolve to null in a packaged module by SELFish's deliberate design (obscene#D248), so it is defined as an empty function. **Something calls `oops_gl_create` as of 2026-09-17** (worklog 043): `oops-apps/src/oops-mesa/dri-probe`, title `DRIP00001`, a second title beside `mesa-probe` because the frontend creates its own screen and one title doing both would have two. It goes as far as `glGetString`, which separates a context that never became current from one that cannot dispatch. Writing it found that **no archive in this build defined a single public `gl*` name** - they live in upstream's `libglapi_bridge`, which is `build_by_default : false` because only libGL wants it - and that the manifest had placed `glGetString` in `libSceGLSlimServerVSH`, the platform's *own* OpenGL, while reporting 0 unknown. The archive is now built and linked whole, and `generate-imports.sh` refuses any undefined symbol in Mesa's namespace outright. **`oops_gl_create` ran on hardware on 2026-09-19** (worklog 049): the frontend declined at `driCreateNewScreen3` before any ioctl, because `pipe_loader_drm_probe_fd` dups the device fd and `oops_winsys_open` had returned a synthetic token that `fcntl(F_DUPFD_CLOEXEC)` cannot duplicate. Fixed in the winsys - a real dup-able fd from `dup(stderr)`, ioctl gate now serves the fd's dups - and built, but **not yet verified**: the parked instance holds the slot and `pros` cannot close it (`-b1e4`), so the next launch waits on the slot being freed by the shell's own Close or a reboot. **patch 003 is verified on hardware 2026-09-19** (worklog 050): with `os_dupfd_cloexec` passing the device fd through when the platform's `fcntl(F_DUPFD)` returns EINVAL - measured to fail even on a real open file - the frontend gets past the dup and **drives radeonsi through its whole device init: 39 ioctls, the same path `mesa-probe` walks, to a `pipe_screen`.** The crash moved deep into new ground: a null-pointer write in `dri_init_screen` -> `dri_fill_in_modes` (framebuffer-config enumeration), the step between "the driver initialised" and "a GL context can be made" - located to function level via the link map, line pending a `-g` recompile of `dri_screen.c`. The other open unknown is unchanged: whether `sceVideoOutRegisterBuffers2` constrains a buffer's address, which needs a surface to exist before it can be asked. **That question is answered, and the answer was reached by asking it** - a surface exists, the flip works, and the frame is on the panel. Between worklog 050 and 066 the remaining walls fell in order: config enumeration on a heap fix (051-052), an empty high VA range and a `GEM_VA` encoding the dispatch missed (053), the submit path and a fence that would not retire until `sceAgcInit` ran (054-056), and then three separate causes of one `ILLEGAL_INST` - not CPU-write staleness (057), a shader in no buffer (058), two buffers sharing a CPU address (059) - ending in the one that mattered: **ACO's opcode table was all zeroes because `.init_array` had never run** in a hosted title with no crt (061-062), fixed with `oops_mesa_run_init_array()` and the matching output sections in the title's link script. Presentation then crashed at `rip: 0` because `driSwapBuffers` is swrast/kopper-only and null for an image loader; the DRI2 flush is `dri_flush_drawable` (063). **A clear read back pixel-exact (064) and the first frame reached the display (065)**: `dri_flush_drawable`, `glReadPixels(GL_BGRA)` to detile, a row swap for scanout order, and `oops_display_present`. radeonsi's GPU use and oops-sdk's display coexist because the display runs its CPU tiler, off the queue radeonsi drives. **The programmable pipeline runs as of 2026-09-20** (worklog 066, [record](hardware/glsl-pipeline-and-frame-hash-fw1240.md)): GLSL 330 compiles, links and draws, with the centre pixel matching the barycentric prediction exactly (64, 64, 128) and coverage matching the analytic triangle area exactly (373248 px). Getting there cost one more fault, and it was ours rather than Mesa's: Mesa's C11 layer always calls `pthread_create` with a **null attribute**, so its threads took the vendor's default stack, and NIR's varying optimiser overflowed it about 72 KB down - now fixed in the runtime shim, which gives an attribute-less caller 8 MB. **What is left on this unit is the gate itself, both halves.** The frame hash exists (`0x5188ddb7`) but has been produced once, and a gate needs it stable, so one repeat run closes it. The swap is still the CPU path: `flip 1: tile=8077 submit=64 total=8141` is 8.14 ms for the display half alone, with the `glReadPixels` detile on top of it and unmeasured - `REQ-7e21` (render straight into a scanout buffer) is what removes both |
| 7 | the static SDK: `dist/` with headers, archives and `oops-mesa.mk`; the `USE_MESA` switch in oops-apps' `app.mk`; an example title | a title in oops-apps builds with `make title`, packages with SELFish, deploys with Prosperous, and renders | builds and packages 2026-09-15 (`oops-apps/src/oops-mesa/mesa-probe`, worklog 015); **deploying works as of 2026-09-17** - `make title`, SELFish, `pros restore`, and the console loads and runs it (worklog 038). The title identifier has to be four letters and not a vendor prefix, which `app.mk` now enforces. **Rendering no longer waits on anything**: `dri-probe` builds with `make title`, packages with SELFish, deploys with `pros restore` and renders both fixed-function and GLSL frames to the panel (worklog 065 and 066). `app.mk` gained the one thing a hosted title needed that a freestanding one does not - its C library comes from the Mesa sysroot, not oops-sdk's freestanding one, whose `clock_t` collides with it (`int64_t` against the sysroot's `int`) - so `USE_MESA` now guards the libc include and the `libc.c`/`math.c`/`scanf.c` sources. What is left here is the example title being an example rather than a probe |
| 8 | conformance: a bounded, named subset of the GL 3.3 CTS run on the hardware, results recorded as data | the subset and its pass table are in `docs/hardware/` with the build and firmware named | |

## What is deliberately not on it

- Any OpenGL entry point, shading-language work, or gallium driver code written here (D001).
- Firmware or console support claims beyond the one unit at this desk on firmware 12.40. Every
  hardware statement names its firmware, as the prior art does for 6.02 and 12.02.
- A desktop compatibility profile. Titles adapt their entry point, input and lifecycle to
  oops-sdk; the SDL2 question is a later decision if a port needs it.

## Requests already on the obSCEne bus that this project consumes

`REQ-20260914T1206Z-a1c3` (submit descriptor), `-b7e4` (initialisation gate),
`-1221Z-c5d9` (shader container), `-1245Z-e8a1` (register defaults) and `-1245Z-f3b2`
(linear pitch).

`REQ-20260914T1443Z-3ea7` is filed and is an **optimisation, not a gate**: does `libkernel`
serve the portable thread names on the app-sandbox leg, under either spelling, so the mapping
table can be skipped? Unit 4 no longer waits on it. Its thread surface is closed and every name
in it has a vendor twin, written down in
[the thread surface table](hardware/mesa-thread-surface-fw1240.md) with the arity trap that
comes with delegating. A negative answer costs nothing; a positive one makes unit 4 smaller.
